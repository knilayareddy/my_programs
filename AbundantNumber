num = int(input("Enter any number:"))
sum = 0
for i in range(1,num):
    if(num % i == 0):
				sum = sum + i
print(sum)
if(sum > num):
		print(f"Given number {num} is an abundant number")
else:
		print(f"Given number {num} is not an abundant number")
